# genomes and annotation output are large and reproducible — never commit
data/genomes/
data/annotations/
data/interim/
*.fna
*.gbff
*.gff3
*.faa
*.tsv.gz
*.csv.gz

# bakta database
db/
*.db

# python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
env/

# os
.DS_Store

# notes/ is published: the lab notebook is part of the record, not scratch.
# (An entry for notes/lab_notebook.md used to sit here. It never had any
# effect -- the file was already tracked -- and it made the file look
# unintentionally published. Removed rather than left to mislead.)